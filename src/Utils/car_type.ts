export type carAdd_Props = {
  name: string;
  description: string;
  image: string;
  price: number;
};

export type car_Props = {
  id: string;
  name: string;
  description: string;
  image: string;
  price: number;
};
